 {{#each erros}}
   <div class="alert alert-danger">{{texto}}</div>
{{else}}

{{/each}}
<div class="card">
   <div class="card-body">
      <h3>Nova categoria: </h3>
      <form action="/routes/admin/categorias/nova" method="POST">
         <label for="nome">Nome: </label>
            <input type="text" name="nome" id="nome" placeholder="Nome das categorias" class="form-control" required>
         <label for="nome">Slug: </label>
            <input type="text" name="slug" id="slug" placeholder="Slug das categorias" class="form-control" required>
            <br>
         <button type="submit" class="btn btn-success">Criar categoria</button>
      </form>
   </div>
</div>